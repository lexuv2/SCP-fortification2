finalcode   = string_char_at(code, irandom(61)); // random letter
//extrac      = string_char_at(code, irandom(61)); // random character to be made 
// ^ Uncomment to have random character for the extra character (Remove Alarm 1 too)

/* Random sector (always updating) */
finalsector = string_char_at(code, irandom(61)) + string_char_at(code, irandom(61)) + string_char_at(code, irandom(61)) + string_char_at(code, irandom(61)) + string_char_at(code, irandom(61)) ;